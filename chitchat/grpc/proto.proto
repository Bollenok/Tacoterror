syntax = "proto3";
option go_package = "chitchat/grpc/proto";

service ChitChat {
  // one stream per client connection
  rpc Chat(stream ClientMessage) returns (stream ServerMessage);
}

message ClientMessage {
}

message ServerMessage {
  
}

// will still receive it if empty
message Empty {}
